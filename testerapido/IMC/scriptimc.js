//peso / altura*altura
/*let pessoa = {
    nome:'kaynã',
    sobrenome:'Silva',
    idade:21,
    peso:110,
    altura:1.90,
    
}
let imc = pessoa.peso / (pessoa.altura*pessoa.altura)

console.log(`${pessoa.nome} ${pessoa.sobrenome} tem ${pessoa.idade} anos, pesa ${pessoa.peso} kg tem ${pessoa.altura} de altura e seu IMC é de ${imc}`)*/